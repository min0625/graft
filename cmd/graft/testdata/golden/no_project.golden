$ graft lock
error: graft.toml not found

  run `graft init <vendor>` first
[exit 2]

$ graft apply
error: graft.toml not found

  run `graft init <vendor>` first
[exit 2]

$ graft status
error: graft.toml not found

  run `graft init <vendor>` first
[exit 2]

$ graft add github.com/org/repo@v1.0.0
error: graft.toml not found

  run `graft init <vendor>` first
[exit 2]

$ graft remove repo
error: graft.toml not found

  run `graft init <vendor>` first
[exit 2]

